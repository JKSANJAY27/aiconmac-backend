services:
  - type: web
    name: aiconmac-backend
    env: node
    buildCommand: npm ci && npx prisma generate && npx prisma db push --accept-data-loss
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
